"use strict";
const { G4F } = require("g4f");
const g4f = new G4F();
module.exports.run = async (bot, args, message, lang) => {
    
}
module.exports.config = {
    name: "imageai",
    aliases: ["ai"],
    permissions: [],
    disable: false,
    cooldown: 5
}